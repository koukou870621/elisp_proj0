
(defun main ()
  (setq i (float 5))
  (let ((i 0.0)))
  (let ((bonus1 0.0)
        (bonus2 0.0)
        (bonus4 0.0)
        (bonus6 0.0)
        (bonus10 0.0)
        (bonus 0.0)))
  (message "あなたの純利益は: \n")
  (let ((i (read-string "Enter a string: ")))))



(my-scanf-demo)

(main)
